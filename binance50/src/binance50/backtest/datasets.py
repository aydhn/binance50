import pandas as pd


def backtest_events_to_dataframe(events) -> pd.DataFrame:
    return pd.DataFrame()


def backtest_fills_to_dataframe(fills) -> pd.DataFrame:
    return pd.DataFrame()


def backtest_positions_to_dataframe(positions) -> pd.DataFrame:
    return pd.DataFrame()


def backtest_trades_to_dataframe(trades) -> pd.DataFrame:
    return pd.DataFrame()


def backtest_equity_to_dataframe(equity_curve) -> pd.DataFrame:
    return pd.DataFrame()


def backtest_metrics_to_dataframe(metrics) -> pd.DataFrame:
    return pd.DataFrame()
