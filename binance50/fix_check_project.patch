--- scripts/check_project.py
+++ scripts/check_project.py
@@ -194,6 +194,15 @@
         return False
     print(f"  {success_icon} 'doctor' passed.")

+    print("\nNetwork Safety (Phase 6) CLI tests:")
+    if not run_command(["python", "-m", "binance50.cli", "rate-limit-status"], check=True):
+        return False
+    if not run_command(["python", "-m", "binance50.cli", "recv-window-check"], check=True):
+        return False
+    if not run_command(["python", "-m", "binance50.cli", "websocket-limits-check", "spot", "10", "1"], check=True):
+        return False
+    print(f"  {success_icon} Network safety CLI tests passed.")
+
     print(f"\n{success_icon} CLI structure tests passed.")
     return True
